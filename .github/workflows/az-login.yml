name: test-az-login
on:
  workflow_dispatch:
  
jobs:
  test-az-login:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Echo secret
        run: |
          echo ${{ secrets.TEST_IDENTITY01 }}
