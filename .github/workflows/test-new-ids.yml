name: test-new-ids

on:
  workflow_dispatch:
  
jobs:
  test-path-login:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: "Deploy path"
        run: |
          for filepath in ./config/config01/*; do
            echo "File: $(basename "$filepath")"
            echo "Path: $filepath"
          done


