name: List Files in github/config

on:
  workflow_dispatch:
  
jobs:
  list-files:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: List files and directories
        run: |
          ls -l github/config




